<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TiffinWaale.Views.SupplierContactPage"
             xmlns:c="clr-namespace:TiffinWaale.Controls"
             Title="Contact">
    <ContentPage.Content>
        <Grid Padding="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="10"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Label Text="{Binding Supplier.Name}" Grid.Row="0"/>
            <c:CustomMap x:Name="TiffinMap"
                     Items="{Binding SupplierPin}"
                     WidthRequest="320"
                     IsShowingUser="True"
                     MapType="Street"
                     Grid.Row="1"/>
            <Label Text="{Binding Supplier.Address}" Grid.Row="2"/>
            <Label x:Name="PhoneNumber" Text="{Binding Supplier.PrimaryPhone}" Grid.Row="3"/>
            <Button x:Name="callButton" Text="Call" Clicked="OnCall" Grid.Row="3" />
        </Grid>
    </ContentPage.Content>
</ContentPage>